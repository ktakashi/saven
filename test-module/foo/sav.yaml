---
dependencies:
  - github:
      name: ktakashi/r6rs-pffi
      paths:
        - src
targets:
  - name: run
    process:
      name: sagittarius
      arguments:
        - '@{load-paths,-L}' # expanded to list of arguments with -L prefix
        - '${output-directory}/run-foo.scm'
        - '${load-paths}' # comma separated
  - name: run
    process:
      name: sagittarius
      arguments:
        - '@{load-paths,-L}' # expanded to list of arguments with -L prefix
        - '${output-directory}/run-foo.scm'
        - '${load-paths,:}  ${target-directory}'
