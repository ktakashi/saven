---
dependencies:
  - github:
      name: ktakashi/r6rs-pffi
      paths:
        - src
targets:
  - name: run
    process:
      name: sagittarius
      arguments:
        - '@{load-path,-L}' # expanded to list of arguments with -L prefix
        - '${target-directory}/foo/run-foo.scm'
        - '${load-path}' # comma separated
